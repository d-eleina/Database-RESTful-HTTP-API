# Database-RESTful-HTTP-API

A server that stores data on the computer. Includes minimal CRUD interface.
It can run on any computer with a recent version of node and npm installed.
It can be installed using "npm install" and the server can be started by using the "npm start" command.
This repository also contains a folder with a website that operates using the server. This website supports all of the operations possible with the server.
